# TRINETRA Frontend Environment Variables

# Base URL pointing to deployed TRINETRA Backend (Railway, Render, AWS, Docker)
# Leave unset or empty for default local fallback (http://localhost:8000)
VITE_API_URL=https://your-backend-api.railway.app
